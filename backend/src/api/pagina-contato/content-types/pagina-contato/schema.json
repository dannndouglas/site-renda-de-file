{
  "kind": "singleType",
  "collectionName": "pagina_contato",
  "info": {
    "singularName": "pagina-contato",
    "pluralName": "paginas-contato",
    "displayName": "Página Contato",
    "description": "Conteúdo da página de contato"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "titulo_pagina": {
      "type": "string",
      "default": "Entre em Contato",
      "required": true
    },
    "subtitulo_pagina": {
      "type": "string",
      "default": "Estamos aqui para ajudar e responder suas dúvidas"
    },
    "imagem_fundo_cabecalho": {
      "type": "media",
      "multiple": false,
      "required": false,
      "allowedTypes": [
        "images"
      ]
    },
    "posicao_imagem_fundo": {
      "type": "enumeration",
      "enum": [
        "Centro",
        "Centro Superior",
        "Centro Inferior",
        "Esquerda Centro",
        "Direita Centro",
        "Esquerda Superior",
        "Direita Superior",
        "Esquerda Inferior",
        "Direita Inferior"
      ],
      "default": "Centro",
      "required": false
    },
    "opacidade_overlay": {
      "type": "enumeration",
      "enum": [
        "Sem Overlay (0%)",
        "Overlay Leve (25%)",
        "Overlay Médio (50%)",
        "Overlay Forte (75%)",
        "Overlay Intenso (100%)"
      ],
      "default": "Sem Overlay (0%)",
      "required": false
    },
    "conteudo_introducao": {
      "type": "blocks",
      "required": false
    }
  }
}
